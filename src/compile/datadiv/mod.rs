//! Data Division compilation.

use crate::src::Error;

use super::tkn::Muncher;

/// The compiled form of the data division.
pub struct DataDiv {}

/// Compile the data division.
pub fn compile(m: &mut Muncher) -> Result<DataDiv, Error> {
    todo!()
}
